export type UserDetails = {
  email: string;
  displayName: string;
};

export type MicUserDetails = {
  firstName: string;
  lastName: string;
  email: string;
  // picture: any;
};

export const STRIPE_CLIENT = 'STRIPE_CLIENT';
